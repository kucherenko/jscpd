---
title: JSON Reporter
description: Generate JSON reports for CI/CD integration.
navigation:
  icon: i-lucide-braces
seo:
  title: JSON Reporter
  description: Generate JSON duplication reports with jscpd for CI/CD pipelines.
---

## Usage

```bash
jscpd --reporters json ./src
```

## Output

The JSON reporter generates a `jscpd-report.json` file with structured data:

```json
{
  "duplicates": [
    {
      "format": "javascript",
      "lines": 10,
      "tokens": 120,
      "firstFile": {
        "name": "src/utils.js",
        "start": 10,
        "end": 20
      },
      "secondFile": {
        "name": "src/helpers.js",
        "start": 5,
        "end": 15
      }
    }
  ],
  "statistics": {
    "total": {
      "lines": 1000,
      "tokens": 15000,
      "sources": 50,
      "clones": 5,
      "duplicatedLines": 50,
      "percentage": 5.0
    }
  }
}
```

## CI/CD Integration

Use JSON output for automated pipelines:

```yaml [.github/workflows/jscpd.yml]
- name: Check for duplicates
  run: |
    npx jscpd --reporters json --threshold 5 ./src
    cat jscpd-report.json
```
