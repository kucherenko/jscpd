# Note: rust/ is intentionally excluded from the pnpm workspace.
# The Rust npm package (cpd) is managed independently via GitHub Actions.
packages:
  - apps/*
  - packages/*

onlyBuiltDependencies:
  - '@parcel/watcher'
  - better-sqlite3
  - classic-level
  - core-js-pure
  - esbuild
  - sharp
  - vue-demi
