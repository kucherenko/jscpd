{
  "name": "jscpd-repo",
  "private": true,
  "version": "5.1.2",
  "description": "jscpd repository root. Not published: exists so `pre-commit` (language: node) can install the repository and get the `jscpd` binary; the engine lives in rust/.",
  "homepage": "https://jscpd.dev",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/kucherenko/jscpd.git"
  },
  "license": "MIT",
  "dependencies": {
    "jscpd": "5.1.2"
  },
  "engines": {
    "node": ">=20"
  }
}
