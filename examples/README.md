# Examples

| Directory | What it shows |
|-----------|---------------|
| [`api/`](api) | The Node.js programming API: `jscpd()` and `detectClones()` from the `jscpd` / `@jscpd/core` packages, a LevelDB persistent store, and a GitHub workflow that runs jscpd and comments on pull requests. |

For the CLI, pre-commit hooks and CI usage see [`docs/`](../docs) — in particular
[`docs/typescript.md`](../docs/typescript.md) and [`docs/ci-and-hooks.md`](../docs/ci-and-hooks.md).
