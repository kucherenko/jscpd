{
 "name": "@jscpd/core",
 "version": "3.3.0",
 "main": "src/index",
 "types": "src/index",
 "files": [
  "dist"
 ],
 "scripts": {
  "build": "npm run clean && npm run compile",
  "clean": "rm -rf ./dist",
  "compile": "tsc -p tsconfig.build.json",
  "prepublishOnly": "npm run build"
 },
 "dependencies": {
  "eventemitter3": "^4.0.0"
 }
}
